name: "CodeQL Config: Игнорируем кринж"

paths-ignore:
  - '**/test/**'  # Игнорируем тестовые файлы
  - '**/vendor/**' # Игнорируем зависимости

queries:
  - uses: security-extended
  - uses: security-and-quality
